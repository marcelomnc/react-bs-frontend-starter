import React from "react";
import * as Labels from "../../../shared/ui/Labels";

const Footer = (props) => {
	return <footer className="Footer">{Labels.footerLabel}</footer>;
};

export default Footer;
